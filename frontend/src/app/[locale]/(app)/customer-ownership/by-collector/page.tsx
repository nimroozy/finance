"use client";
import { PageSection } from "@/components/ui/page-section";
export default function ByCollectorPage() {
  return <PageSection title="Customers by collector" description="Open a collector from permanent ownership or use the API by-collector endpoint."><p className="text-sm text-muted">Use Permanent ownership filters or collector workload views.</p></PageSection>;
}
