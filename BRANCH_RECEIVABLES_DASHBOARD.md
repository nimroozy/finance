# Branch receivables dashboard

API: `GET /api/v1/reports/branch-receivables`  
UI: `/reports/branch-receivables`

Per branch: receivable from open Zoho invoice balances, debtors, owned/temp/unassigned counts, collector amounts, collections today/month, cash held, pending handovers, overdue promises, sync freshness.
