# Cash Reconciliation

`POST /api/v1/cash-reconciliations/run` builds daily runs comparing opening cashbox balance, approved handovers, transfers, bank deposits, and adjustments. Differences are stored for review — never auto-forced to match.
