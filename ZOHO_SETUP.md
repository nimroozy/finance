# Zoho Books setup

Stage 2 — not configured in Stage 1.

Required environment variables (see `.env.example`):

- `ZOHO_CLIENT_ID`
- `ZOHO_CLIENT_SECRET`
- `ZOHO_REDIRECT_URI`
- `ZOHO_ORGANIZATION_ID`
- `ZOHO_ACCOUNTS_DOMAIN`
- `ZOHO_API_DOMAIN`

Admin connection UI and sync jobs arrive in Stage 2.
